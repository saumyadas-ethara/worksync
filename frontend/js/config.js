/* ============================================================
   config.js – Set your backend URL here for local dev.
   In production, set window.ENV_API_URL before loading this file,
   or use Railway's static site env injection.
   ============================================================ */

// Change this to your deployed Railway backend URL when deploying:
// window.ENV_API_URL = 'https://your-backend.railway.app/api';

// For local dev this file is a no-op; api.js falls back to localhost:5000
